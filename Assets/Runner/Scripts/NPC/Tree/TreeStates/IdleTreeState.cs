using UnityEngine;

namespace Runner.NonPlayerCharacters.StateMachine
{
    public class IdleTreeState : TreeState
    {
        public IdleTreeState(TreeStateMachine treeStateMachine) : base(treeStateMachine)
        {
        }
    }
}
